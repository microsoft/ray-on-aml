name: CI and Integration Test 
on:
  push:
    branches: 
      - master
    paths:
      - .github/workflows/ci.yml
      - src/**
      - tests/automated_tests/**
jobs:
  CI_And_Integration_Test:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Setup python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.8' # Version range or exact version of a Python version to use, using SemVer's version range syntax
      - name: Upgrade pip
        run: |
          python -m pip install --upgrade pip
          python -m pip install --upgrade build
          python -m pip install --upgrade twine
      - name: AZ Login
        uses: azure/login@7ddb5af1ef8758cf1353cf3b42f940aee27ba21c # v3.0.2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Install az ml & set default values for AML
        run: |
          az extension add -n ml -y 
          az configure --defaults group=${{ vars.AZURE_RESOURCE_GROUP }} workspace=${{ vars.AZURE_WORKSPACE_NAME }} location=${{ vars.AZURE_LOCATION }}
      - name: run ray rl test
        run: |
          az ml environment create -g ${{ vars.AZURE_RESOURCE_GROUP }} -w ${{ vars.AZURE_WORKSPACE_NAME }} -f tests/automated_tests/test_rl/env.yml
          run_id=$(az ml job create -f tests/automated_tests/test_rl/job.yml --query name -o tsv)
          if [[ -z "$run_id" ]]
          then
            echo "Job creation failed"
            exit 3
          fi
          status=$(az ml job show -n $run_id --query status -o tsv)
          if [[ -z "$status" ]]
          then
            echo "Status query failed"
            exit 4
          fi
          running=("Queued" "Starting" "Preparing" "Running" "Finalizing")
          while [[ ${running[*]} =~ $status ]]
          do
            sleep 8 
            status=$(az ml job show -n $run_id --query status -o tsv)
            echo $status
          done
          if [[ "$status" = "Failed" ]]  
          then
            echo "Test case failed"
            exit 3
          fi
      - name: run ray ml test
        run: |
          az ml environment create -g ${{ vars.AZURE_RESOURCE_GROUP }} -w ${{ vars.AZURE_WORKSPACE_NAME }} -f tests/automated_tests/test_ray_ml/env.yml
          run_id=$(az ml job create -f tests/automated_tests/test_ray_ml/job.yml --query name -o tsv)
          if [[ -z "$run_id" ]]
          then
            echo "Job creation failed"
            exit 3
          fi
          status=$(az ml job show -n $run_id --query status -o tsv)
          if [[ -z "$status" ]]
          then
            echo "Status query failed"
            exit 4
          fi
          running=("Queued" "Starting" "Preparing" "Running" "Finalizing")
          while [[ ${running[*]} =~ $status ]]
          do
            sleep 8 
            status=$(az ml job show -n $run_id --query status -o tsv)
            echo $status
          done
          if [[ "$status" = "Failed" ]]  
          then
            echo "Test case failed"
            exit 3
          fi
      - name: run ray spark test
        run: |
          az ml environment create -g ${{ vars.AZURE_RESOURCE_GROUP }} -w ${{ vars.AZURE_WORKSPACE_NAME }} -f tests/automated_tests/test_spark/env.yml
          run_id=$(az ml job create -f tests/automated_tests/test_spark/job.yml --query name -o tsv)
          if [[ -z "$run_id" ]]
          then
            echo "Job creation failed"
            exit 3
          fi
          status=$(az ml job show -n $run_id --query status -o tsv)
          if [[ -z "$status" ]]
          then
            echo "Status query failed"
            exit 4
          fi
          running=("Queued" "Starting" "Preparing" "Running" "Finalizing")
          while [[ ${running[*]} =~ $status ]]
          do
            sleep 8 
            status=$(az ml job show -n $run_id --query status -o tsv)
            echo $status
          done
          if [[ "$status" = "Failed" ]]  
          then
            echo "Test case failed"
            exit 3
          fi
      - name: run ray data test
        run: |
          az ml environment create -g ${{ vars.AZURE_RESOURCE_GROUP }} -w ${{ vars.AZURE_WORKSPACE_NAME }} -f tests/automated_tests/test_ray_data/env.yml
          run_id=$(az ml job create -f tests/automated_tests/test_ray_data/job.yml --query name -o tsv)
          if [[ -z "$run_id" ]]
          then
            echo "Job creation failed"
            exit 3
          fi
          status=$(az ml job show -n $run_id --query status -o tsv)
          if [[ -z "$status" ]]
          then
            echo "Status query failed"
            exit 4
          fi
          running=("Queued" "Starting" "Preparing" "Running" "Finalizing")
          while [[ ${running[*]} =~ $status ]]
          do
            sleep 8 
            status=$(az ml job show -n $run_id --query status -o tsv)
            echo $status
          done
          if [[ "$status" = "Failed" ]]  
          then
            echo "Test case failed"
            exit 3
          fi
      - name: run ray xgboost-ray test
        run: |
          az ml environment create -g ${{ vars.AZURE_RESOURCE_GROUP }} -w ${{ vars.AZURE_WORKSPACE_NAME }} -f tests/automated_tests/test_xgboost_ray/env.yml
          run_id=$(az ml job create -f tests/automated_tests/test_xgboost_ray/job.yml --query name -o tsv)
          if [[ -z "$run_id" ]]
          then
            echo "Job creation failed"
            exit 3
          fi
          status=$(az ml job show -n $run_id --query status -o tsv)
          if [[ -z "$status" ]]
          then
            echo "Status query failed"
            exit 4
          fi
          running=("Queued" "Starting" "Preparing" "Running" "Finalizing")
          while [[ ${running[*]} =~ $status ]]
          do
            sleep 8 
            status=$(az ml job show -n $run_id --query status -o tsv)
            echo $status
          done
          if [[ "$status" = "Failed" ]]  
          then
            echo "Test case failed"
            exit 3
          fi
